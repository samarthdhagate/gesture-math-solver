import pyttsx3

engine = pyttsx3.init()
engine.setProperty('rate', 160)
engine.setProperty('volume', 0.9)

def speak(text):
    """Speaks the given text aloud."""
    engine.say(text)
    engine.runAndWait()

